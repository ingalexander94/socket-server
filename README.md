# Socket-Server

    Reconstruir modulos de Node

    ```
    npm install
    ```

    Generar el Dist

    ```
    tsc -w
    ```

    levantar el servidor puerto 5000 con cualquiera de estos comandos
    
    ```
    nodemon dist/
    node dist/
    ```

